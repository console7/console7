# RISKS register — tracked technical debt & calibrated exceptions

This register satisfies **CO-17** of the
[Console7 Repository SDLC Standard](standards/console7-sdlc-standard.md): *"tech debt
tracked in a RISKS register."* It records every **conscious** deviation from a
quality gate — a relaxed threshold, an excluded lint rule, a `//nolint`, an accepted
gap — so debt is **tracked, never silently absorbed** (the project's stance on
itself: a deviation is a regression to be justified, not a free trade-off).

Scope: this register is about the **engineering of this repo**. Product-level accepted
gaps live in the SDLC standard §5 (Tracked targets); session/product risks live in
`docs/THREAT-MODEL.md`. Keep entries terse; link the code and the gate each touches.

| ID | Date | Area | The exception | Why it is acceptable | Revisit when |
|----|------|------|---------------|----------------------|--------------|
| R-1 | 2026-06-21 | Lint / gosec (CO-17, CO-7.1) | `gosec` rule **G115** (integer-overflow on conversion) is excluded in [`.golangci.yml`](../.golangci.yml). | Every G115 site in the tree is a safe-by-construction conversion in a length-prefix codec (`appendField`/`takeField` in `keybroker/signing/ca_dev.go`, `providers/secrets-gcp/ports.go`) or a record count (`control-plane/evidence` `Sink.Len`). The lengths are KB-scale and bounded well under 2³², never attacker-sized to overflow. (The same idiom recurs in the matching `_test.go` files — e.g. `control-plane/evidence/evidence_test.go` — and is covered by the same tree-wide exclusion.) G115 is high-noise on exactly this idiom. **CodeQL remains the SAST of record** for genuine overflow-driven vulnerabilities; all other `gosec` rules stay on. | A conversion of an *externally-sized* quantity (a network/file length read into a narrower type without a bound) is introduced — then re-enable G115 and guard the specific site instead. |
| R-2 | 2026-06-21 | Trivy / terraform config (CO-17, CO-7.1) | `trivy` rule **GCP-0077** (bucket access logging) is ignored inline on the evidence bucket (`#trivy:ignore:AVD-GCP-0077` in [`deploy/gcp/modules/evidence/main.tf`](../deploy/gcp/modules/evidence/main.tf)). | GCP-0077 wants the **legacy** Cloud Storage bucket access/usage logs delivered to a target log bucket. The right control for auditing access to the evidence bucket is **Cloud Audit Logs (Data Access)** — a project/org-level configuration, not a bucket resource attribute — which records reads/writes with identity and is GCP-recommended over the older best-effort access logs. Wiring legacy logs would also require a second log bucket that itself re-triggers GCP-0077 (recursion). The bucket is otherwise hardened (uniform access, public-access-prevention, retention policy); the ignore is scoped to this one bucket with an inline reason. | The module gains a dedicated log-sink bucket for another reason, or an adopter requires legacy access logs specifically — then add a `logging{}` block (and ignore GCP-0077 on the log bucket only). |
| R-3 | 2026-06-21 | Lint / gosec (CO-17, CO-7.1) | `gosec` rule **G204** (subprocess launched with non-constant args) is suppressed at ONE audited site — `kubeRunner.run` in [`providers/cloud-gcp/kube_exec.go`](../providers/cloud-gcp/kube_exec.go) (`//nolint:gosec // G204 … RISKS R-3`). | The reference `CloudProvider` adapter realises its ports by shelling out to the adopter's pinned `kubectl` + `gcloud` — the deliberate **zero-dependency** choice (Option A) that keeps this Tier-1 **public** SDK module free of `k8s.io/client-go`'s large transitive graph. G204 fires because the args are not string literals, but the command **name** is always a literal (`"kubectl"`/`"gcloud"`) and the only variable inputs are **validated `Config` fields + the provider's own crypto-random handle IDs**, never external/attacker input; manifests are passed over stdin, not as args. Every subprocess routes through this single helper, so the suppression is one audited line, not scattered. CodeQL remains the SAST of record. | The adapter is changed to swap to a typed Kubernetes client (e.g. a nested module embedding `client-go`, the documented Option B), removing the shell-out — then drop this site and the suppression. Or, if a future arg ever derives from untrusted input, re-enable G204 and guard that input explicitly. |
| R-4 | 2026-06-21 | Coverage floor (CO-17) | The `providers/cloud-gcp` coverage floor in [`scripts/coverage-gate.sh`](../scripts/coverage-gate.sh) was lowered **67 → 64**, then ratcheted **64 → 70** (2026-06-22, as B5/B6 added CI-unit-tested logic), staying below full only for the integration-only shell-out adapter in [`kube_exec.go`](../providers/cloud-gcp/kube_exec.go). | `kubeEngineRunner.Run`/`execIn`/`gitExec`, `kubeCredentialDeliverer.Deliver`/`Wipe`, and the workspace-seed exec shell out to `kubectl` and the in-pod engine, so — exactly like the existing `kubeRuntime`/`netpolEgressController` adapters the floor already accounts for — they are exercised only by the opt-in, cluster-bound integration test (`-tags cloud_gcp_integration`), never by CI. The **seam's contract logic** (the liveness/taint gate around `RunTask`, the fail-closed paths, the `Owns`/`DeliverIfOwned`/`denyDeliverer` ownership gate, the `EngineResult` shape) is fully covered by the in-memory fakes + white-box tests; the pure helpers (`nonEmptyLines`, `workspaceSeedScript`/`shquote`/`isProtectedBranch`) are unit-tested (`injector_test.go`, `seed_test.go`). Only the un-unit-testable shell-out lines are uncovered, the same CI blind spot R-3 already documents for this adapter. | The shell-out adapter is replaced by a typed client (Option B, see R-3) that is unit-testable, or a real-cluster coverage signal is wired into CI — then ratchet the floor back up. |
| R-5 | 2026-06-22 | Lint / gosec (CO-17, CO-7.1) | `gosec` rule **G101** (hardcoded credentials) is suppressed at ONE site — `credentialPath` in [`providers/cloud-gcp/kube_exec.go`](../providers/cloud-gcp/kube_exec.go) (`//nolint:gosec // G101 … RISKS R-5`). | G101 flags the assignment because the constant's NAME contains "credential", but its VALUE is the **filesystem path** `"/run/console7/credential"` — the in-pod `medium: Memory` (tmpfs) location the `CredentialDeliverer` writes a delivered credential to — **not** an embedded credential value. There is no secret in source (the actual material is injected at run time over stdin, never in the tree; the repo is public OSS with a secret-scan gate). The suppression is one audited line; CodeQL remains the SAST of record for genuine secret-in-source findings. | The constant is renamed to a non-credential-keyworded identifier, or G101 gains a path-aware exclusion — then drop this site and the suppression. |
| R-6 | 2026-06-22 | Trivy / kubernetes (CO-17, CO-7.1, CO-05) | `trivy` rule **KSV-0125** (image from an untrusted registry) is suppressed, **path-scoped to `deploy/gcp/modules/egress-proxy/proxy.yaml`**, via [`.trivyignore.yaml`](../.trivyignore.yaml) (wired into the gate with the `trivyignores` input in [`terraform.yml`](../.github/workflows/terraform.yml)). | KSV-0125 fires because `docker.io` is not on Trivy's default trusted-registry allowlist — a **registry-trust heuristic, not a vulnerability**. The image is `ubuntu/squid`, **Canonical-maintained** and **DIGEST-PINNED** (`@sha256:…`, content-addressed so the bytes can't change under us), and there is no canonical Squid image on a Trivy-"trusted" registry. An adopter who wants the image under their own trust root **mirrors it into their in-tenancy Artifact Registry** (the same model as the sandbox base-image). The suppression is **path-scoped** (bound to the one file, never a blanket ID), so a future untrusted-registry image elsewhere is still flagged; all other Squid hardening (non-root uid, read-only root FS, dropped caps, RuntimeDefault seccomp) is applied, not suppressed. (k8s-manifest inline `#trivy:ignore` directives are placement-fragile across resources, so the `.trivyignore.yaml` form is used here; Terraform findings stay inline, e.g. R-2.) | The image is referenced from a Trivy-trusted registry (e.g. the adopter mirrors it into their AR and the manifest points there), or Trivy's trusted-registry policy is configured to include the pinned source — then drop the path entry. |
| R-7 | 2026-06-22 | Concurrency / throughput (CO-16, CO-15) | The reference `providers/cloud-gcp` `Provider` holds the single global `p.mu` across blocking `kubectl exec` I/O in `DeliverIfOwned` and `DestroySandbox`'s best-effort `Wipe` (each bounded by `deliverTimeout` 30s), head-of-line-blocking every other session's Provision/Apply/Destroy/Owns for up to that bound. | The package's deliberate atomicity-over-throughput choice, stated at the `Provider` struct doc ([`provider.go`](../providers/cloud-gcp/provider.go)): the same whole-method-under-lock model `providers/secrets-gcp` adopted, with `RunTask` the one documented exception (a run takes minutes; delivery is 30s-capped). It preserves the atomic check-and-deliver ownership invariant and **never fails open**. This is the **reference** CloudProvider, not the shipping multi-tenant control plane. | The concurrent-session orchestration phase arrives — then shard the lock per handle (or release-after-`owns`-recheck, mirroring `RunTask`) and add a test that a hung deliverer does not block `ProvisionSandbox`. |
| R-8 | 2026-06-22 | Resource hygiene (CO-16, CO-17) | The in-memory `Provider.sandboxes` map ([`provider.go`](../providers/cloud-gcp/provider.go)) retains DESTROYED/expired entries for the life of the process (Destroy flips `live=false`; `lookup` lazily expires but neither deletes), so it grows unbounded. | Entries are small fixed structs that accumulate only over an extreme sandbox count against one long-lived process; **no fail-open and no isolation impact** — the security wipe is pod-deletion + `activeDeadlineSeconds`, not this map. Retaining ids also avoids reuse, though the 128-bit crypto-random id space + `freshID` retry already guarantee that. Reference-impl hygiene, same class as R-7. | The concurrent-session phase — then evict destroyed/expired entries past a grace period (or move any audit trail to the Evidence Sink), bounding the map. |
| ~~R-9~~ **(RETIRED 2026-06-25)** | 2026-06-23 | Lint / gosec (CO-17, CO-7.1) | ~~`gosec` rule **G101** (hardcoded credentials) is suppressed at ONE site — `vertexAccessTokenEnv` in `providers/cloud-gcp/kube_exec.go` (`//nolint:gosec // G101 … RISKS R-9`).~~ **RETIRED:** the `CLOUDSDK_AUTH_ACCESS_TOKEN` lever and its `//nolint` are GONE (the revisit trigger fired — see the Why column). | **R-V1 RESOLVED.** Engine 1.0.44's `google-auth-library-nodejs` never read `CLOUDSDK_AUTH_ACCESS_TOKEN`, so the "pre-minted bearer to the engine" lever was dead. The Vertex lane was flipped (F2c-2c) to the documented gateway contract: the engine is pointed at the per-session **auth-proxy** (`ANTHROPIC_VERTEX_BASE_URL` + `CLAUDE_CODE_SKIP_VERTEX_AUTH=1`), which attaches the bearer — **the sandbox holds no Vertex credential**. With the env const removed, the credential-keyworded NAME that triggered G101 no longer exists, so the suppression was deleted with it. (The auth-proxy's own credential-keyworded const names are tracked separately — R-12/R-13.) | **Retired** — nothing to revisit. If a future engine version exposes a consumable pre-minted-bearer env again AND it is reintroduced, file a fresh entry. |
| R-10 | 2026-06-24 | Lint / gosec (CO-17, CO-7.1) | `gosec` rule **G204** (subprocess launched with non-constant args) is suppressed at the two `git` exec sites in [`providers/scm-github/git_cli.go`](../providers/scm-github/git_cli.go) (`gitCLI.run`/`runOut`, `//nolint:gosec // G204 … RISKS R-10`). | The reference `SCMProvider`'s control-plane push→PR bridge realises its `GitTransport` port by shelling out to the adopter's `git` — the same **zero-dependency** Option A as the cloud-gcp adapter (R-3). The command **name** is always the literal `"git"`; the variable inputs are the provider-derived remote URL, a temp bundle path, and a branch that is **seam-validated** by `isSafeRefName` (rejects a leading `-`/option injection and git's forbidden ref metacharacters), with `--` ending option parsing before positionals on `clone`. The installation token reaches git via a credential helper reading `$C7_GIT_TOKEN` from the child env — never argv. CodeQL remains the SAST of record. | The transport swaps to a git library (no shell-out) — then drop these sites and the suppression. |
| R-11 | 2026-06-24 | Lint / gosec (CO-17, CO-7.1) | `gosec` rule **G101** (hardcoded credentials) is suppressed at ONE site — `gitCredHelper` in [`providers/scm-github/git_cli.go`](../providers/scm-github/git_cli.go) (`//nolint:gosec // G101 … RISKS R-11`). | G101 flags the constant because its credential-helper script text contains the word `password`, but the VALUE is a **shell SCRIPT TEMPLATE** (`!f() { … printf 'password=%s' "$C7_GIT_TOKEN"; }`) that READS the token from the child-process env at runtime — there is **no** secret embedded in source. Same class as R-5/R-9 (a credential-keyworded literal whose value is not a secret). One audited line; CodeQL remains the SAST of record + the secret-scan gate covers genuine secrets. | `gosec` gains a content-aware exclusion, or the helper is supplied another way — then drop this site and the suppression. |
| R-12 | 2026-06-25 | Lint / gosec (CO-17, CO-7.1) | `gosec` rule **G706** (log injection via taint analysis) is suppressed at ONE site — the startup log line in [`sandbox/vertex-auth-proxy/main.go`](../sandbox/vertex-auth-proxy/main.go) (`//nolint:gosec // G706 … RISKS R-12`). | G706 flags the `log.Printf` because `addr` and `upstream.String()` flow from `os.Getenv`, which gosec's taint analysis treats as a source. But both are **operator-supplied PROCESS configuration** (`C7_AUTHPROXY_LISTEN` / `CLOUD_ML_REGION` / `C7_AUTHPROXY_UPSTREAM`), set at deploy time by the adopter — not request-derived or attacker-controllable; the upstream is additionally **validated** by `resolveUpstream` (https-absolute, region label rejects host-smuggling chars) before it is logged. The line is emitted **once at startup** and carries **no** request body or token material (the auth-proxy never logs those). One audited line; CodeQL remains the SAST of record. | The logged values ever derive from request input, or gosec gains an env-config-aware exclusion — then re-enable G706 and guard the specific input. |
| R-13 | 2026-06-25 | Lint / gosec (CO-17, CO-7.1) | `gosec` rule **G101** (hardcoded credentials) is suppressed at ONE site — the `envBearerFile` const in [`sandbox/vertex-auth-proxy/main.go`](../sandbox/vertex-auth-proxy/main.go) (`//nolint:gosec // G101 … RISKS R-13`). | G101 flags the const because its value (`"C7_AUTHPROXY_BEARER_FILE"`) contains the credential-keyword substring `bearer`, but the value is an **env-var NAME** — the name of the variable that holds the *path* to the delivered bearer file — **not** a secret embedded in source. Same class as R-9/R-11 (a credential-keyworded literal whose value is not a secret). One audited line; the secret-scan gate covers genuine secrets and CodeQL remains the SAST of record. | `gosec` gains a content-aware exclusion, or the env var is renamed without a credential keyword — then drop the suppression. |

## How to add an entry

1. Give it the next `R-N` id and today's date.
2. Name the gate it relaxes and link the exact code/config.
3. State *why it is safe* and the concrete *revisit* trigger — never "we'll get to it".
4. If the exception is a `//nolint`, the inline comment MUST cite this register id.
