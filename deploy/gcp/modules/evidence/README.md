# `modules/evidence` — GCS bucket-lock WORM (stub)

Stub. Filled by the `evidence-gcs` PR: a retention-locked GCS bucket backing the
evidence `Store` seam (`control-plane/evidence`) for durable WORM, plus a SIEM webhook.
No resources yet.
